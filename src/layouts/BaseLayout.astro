---
import BackToTop from "@/components/common/BackToTop.astro";
import Container from "@/components/common/Container.astro";
import Footer from "@/components/layout/Footer.astro";
import Head from "@/components/layout/Head.astro";
import Header from "@/components/layout/Header.astro";
import "@/styles/app.css";

const { pageSubtitle, pageTitle } = Astro.props;
---

<html lang="en">
  <Head
    description="Alexandre Mouriec's personal website"
    title="Alexandre Mouriec"
  />
  <body
    class="bg-light dark:bg-dark flex min-h-screen flex-col items-center justify-center">
    <main
      class="mx-auto mb-12 flex w-full flex-grow flex-col items-center py-8 text-sm">
      <Header />
      {
        pageTitle && (
          <h1 class="prose mx-auto font-serif text-3xl font-semibold tracking-tight lg:prose-lg xl:prose-xl 2xl:prose-2xl dark:text-white md:text-4xl">
            {pageTitle}
          </h1>
        )
      }
      {
        pageSubtitle && (
          <p class="font-serif text-base italic opacity-60">{pageSubtitle}</p>
        )
      }
      <Container>
        <slot />
      </Container>
    </main>
    <Footer />
    <BackToTop />
  </body>
</html>
