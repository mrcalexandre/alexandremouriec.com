---
import Container from "@/components/common/Container.astro";
import Footnote from "@/components/common/Footnote.astro";
import ProseContent from "@/components/common/ProseContent.astro";
import BaseLayout from "@/layouts/BaseLayout.astro";

const { frontmatter } = Astro.props;
---

<BaseLayout pageSubtitle={frontmatter.subtitle} pageTitle={frontmatter.title}>
  <article class="mx-auto max-w-3xl px-4 sm:px-6 lg:px-8">
    <main class="mt-6">
      <ProseContent class="dark:prose-headings:text-zinc-200">
        <slot />
      </ProseContent>
    </main>

    <hr class="my-6 border-zinc-200 dark:border-zinc-700" />

    <footer class="mb-6">
      <Footnote lastModified={frontmatter.lastModified} />
    </footer>
  </article>
</BaseLayout>
