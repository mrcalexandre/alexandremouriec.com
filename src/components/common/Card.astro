---
const { entry } = Astro.props;
---

<div
  class="border-zinc/15 hover:bg-zinc/5 relative gap-2 rounded-2xl border border-dashed transition-colors duration-300 ease-in-out hover:text-gray-800 dark:border-white/20 dark:hover:bg-white/5 dark:hover:text-gray-200">
  <div class="flex flex-col gap-y-1.5">
    <div class="flex flex-col gap-y-0.5">
      <h1 class="text-lg font-medium">{entry.data.title}</h1>
      <h2 class="text-muted-foreground">{entry.data.description}</h2>
      <h2 class="text-muted-foreground">
        {entry.data.pubDate.toString().slice(0, 16)}
      </h2>
    </div>
    <slot />
  </div>
</div>
