---
const { class: className = "" } = Astro.props;
---

<div
  class={`prose dark:prose-invert prose-headings:font-serif prose-p:font-light prose-li:font-light prose-ul:font-light prose-headings:mb-6 prose-headings:text-zinc-900 prose-h1:text-4xl prose-h2:text-3xl prose-h3:text-2xl prose-h4:text-xl prose-p:text-md prose-p:leading-relaxed prose-p:mb-6 prose-li:mb-2 prose-ul:mb-6 prose-ol:mb-6 prose-blockquote:border-l-4 prose-blockquote:border-zinc-300 prose-blockquote:pl-6 prose-blockquote:italic prose-blockquote:text-zinc-700 dark:prose-blockquote:border-zinc-600 dark:prose-blockquote:text-zinc-300 text-zinc-800 dark:text-zinc-200 prose-img:rounded-lg prose-img:shadow-md prose-img:border prose-img:border-zinc-200 dark:prose-img:border-zinc-700 prose-img:my-8 ${className}`}
>
  <slot />
</div>

<style>
  .prose img {
    display: block;
    margin: 2rem auto;
    max-width: 100%;
    height: auto;
    border-radius: 0.5rem;
    box-shadow:
      0 4px 6px -1px rgba(0, 0, 0, 0.1),
      0 2px 4px -1px rgba(0, 0, 0, 0.06);
    border: 1px solid #e5e7eb;
    transition: all 0.3s ease;
  }

  .dark .prose img {
    border-color: #374151;
    box-shadow:
      0 4px 6px -1px rgba(0, 0, 0, 0.3),
      0 2px 4px -1px rgba(0, 0, 0, 0.2);
  }

  .prose img:hover {
    transform: translateY(-2px);
    box-shadow:
      0 10px 15px -3px rgba(0, 0, 0, 0.1),
      0 4px 6px -2px rgba(0, 0, 0, 0.05);
  }

  .dark .prose img:hover {
    box-shadow:
      0 10px 15px -3px rgba(0, 0, 0, 0.4),
      0 4px 6px -2px rgba(0, 0, 0, 0.3);
  }

  .prose figure {
    margin: 2rem 0;
    text-align: center;
  }

  .prose figure img {
    margin: 0 auto;
  }

  .prose figcaption {
    margin-top: 0.5rem;
    font-size: 0.875rem;
    color: #6b7280;
    font-style: italic;
  }

  .dark .prose figcaption {
    color: #9ca3af;
  }

  @media (max-width: 768px) {
    .prose img {
      margin: 1.5rem auto;
      border-radius: 0.375rem;
    }
  }
</style>
